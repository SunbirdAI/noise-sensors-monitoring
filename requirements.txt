# =============================================================================
# Direct dependencies only.
# Transitive deps (urllib3, certifi, six, attrs, amqp, vine, kombu, …) are
# intentionally omitted — pip resolves them to compatible versions at install
# time.  Pinning transitives causes the cascading conflicts you see on Heroku.
# =============================================================================

# ── Django core ───────────────────────────────────────────────────────────────
Django==3.2.23
gunicorn==21.2.0
whitenoise==6.6.0
dj-database-url==0.5.0
psycopg2-binary==2.9.9
python-dotenv==1.0.1

# ── Django extensions ─────────────────────────────────────────────────────────
django-cors-headers==3.14.0
django-crispy-forms==1.12.0
django-crontab==0.7.1
django-extensions==3.2.3
django-storages==1.14.2
django-taggit==1.5.1
django-timezone-field==5.1

# ── REST API ──────────────────────────────────────────────────────────────────
djangorestframework==3.14.0
drf-spectacular==0.27.2

# ── Celery task queue ─────────────────────────────────────────────────────────
# Celery pulls in vine, amqp, kombu, billiard, click-* automatically.
celery==5.3.6
django-celery-beat==2.6.0
redis==5.0.3

# ── AWS / S3 storage ──────────────────────────────────────────────────────────
# boto3 pulls in botocore, s3transfer, jmespath automatically.
# boto3 1.35+ supports urllib3 2.x on Python ≥ 3.10; 1.34.x did not.
boto3==1.35.49

# ── InfluxDB ──────────────────────────────────────────────────────────────────
influxdb-client==1.40.0

# ── MQTT ──────────────────────────────────────────────────────────────────────
paho-mqtt==1.6.1

# ── Data ──────────────────────────────────────────────────────────────────────
# numpy <2.0 because pandas 2.0.3 does not support numpy 2.x.
numpy>=1.23.2,<2.0
pandas==2.0.3
python-dateutil>=2.8.1,<3.0.0
pytz==2024.1
tzdata==2024.1
requests==2.31.0

# ── Dev / test ────────────────────────────────────────────────────────────────
autopep8==2.0.4
coverage==7.4.3
pytest==8.1.1
pytest-cov==5.0.0
python-crontab==3.0.0
cron-descriptor==1.4.3
